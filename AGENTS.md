# AGENTS.md

## 项目说明

本项目为“勾蓝瑶水龙祠壁画数字交互设计”项目的本地 Web 交互原型。
核心体验路径：
水龙祠建筑 → 建筑布局 → 壁画位置 → 出兵/入将主题 → 右向左观看壁画 → 聚焦“入将图” → 进入自主探索
项目重点是通过滚动驱动的空间与叙事变化，将水龙祠的建筑空间、壁画观看秩序和图像叙事转化为数字交互体验。
详细研究背景、理论依据、设计说明及历史资料不写入本文件，统一参考 `docs/` 下的项目文档。

## 项目架构

本项目由两个相对独立的交互模块组成，属于统一项目，而非两个无关网站。

### 模块 A：水龙祠壁画叙事引导页面（Landing）

负责用户进入项目后的前置叙事体验：
水龙祠开屏 → 建筑空间认知 → 壁画所在位置 → 出兵·入将主题 → 右→左观看秩序 → 壁画整体浏览 → 聚焦《入将图》 → 进入探索

主要采用 Scroll-driven Narrative，通过滚动控制：镜头移动、空间缩放、建筑平面变化、壁画横向移动、信息出现/隐藏、视觉焦点变化。

### 模块 B：《入将图》交互探索（Exploration）

相对独立的交互探索模块，核心功能已基本完成，现有功能以当前项目代码为准。
核心职责：对《入将图》进行自主探索，包括大图浏览、缩放、平移、局部查看、热点、节点、详情信息及其他已有交互。
后续不得为实现模块 A 而重新设计或重构模块 B。

### 模块关系

模块 A（水龙祠叙事引导）→ 《入将图》 → 【进入探索】 → 模块 B（《入将图》交互探索）

- 模块 A 负责“让用户理解为什么来到《入将图》”；模块 B 负责“让用户真正探索《入将图》”。
- 模块 A 可以调用模块 B，但不应依赖模块 B 的内部实现细节（《入将图》的缩放逻辑、热点系统、Canvas/WebGL 核心、大图加载逻辑、节点系统等）。
- 进入模块 B 应尽可能通过明确的入口或路由完成。

## 开发行为规范

### 1. 修改范围控制

每次开发任务开始前，先判断任务属于模块 A、模块 B 还是 shared，优先只修改任务所属模块：
- 修改模块 A 时，默认不得修改模块 B；
- 修改模块 B 时，默认不得修改模块 A；
- 确实需要跨模块修改时，必须先说明原因。
不得因为一个模块的需求而进行无关重构。

### 2. 模块 B 保护规则

《入将图》交互探索模块属于已有核心成果，后续修改必须保留现有核心功能优先。禁止：
- 为了统一代码风格而重构模块 B；
- 为了接入模块 A 而重写模块 B；
- 未经确认修改核心交互；
- 替换现有大图加载方案；
- 删除已有交互；
- 改变已有热点位置；
- 改变已有信息架构。
如果模块 A 只需要一个“进入探索”按钮，优先通过入口/路由连接，而不是修改模块 B 内部结构。

### 3. Git 提交

每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
Commit message 应准确描述本次修改，例如：
feat: add temple opening animation
feat: implement mural scroll narrative
fix: correct right-to-left mural direction
refactor: optimize mural loading
禁止在没有实际修改的情况下创建无意义 commit。

### 4. 测试与验证

每次代码改动后，都必须：
编写或更新与改动相关的测试；
运行相关测试；
进行必要的构建检查；
对视觉或交互改动进行实际运行验证；
在交付给用户前确保所有测试和验证通过。
如果某项验证无法自动化完成，应进行人工验证并明确记录验证结果。
不得在测试失败、构建失败或存在明显运行错误的情况下声称任务已经完成。

### 5. 叙事结构（模块 A）

前导页面采用滚动驱动的连续叙事：
水龙祠整体 → 建筑布局 → 壁画位置 → 出兵/入将 → 右向左观看 → 入将图
进入“入将图”后，再转换为用户自主探索。
不得无理由将前导体验改造成普通的分页网站或图片轮播。

### 6. 壁画观看方向

水龙祠壁画的既定数字叙事方向为：
右 → 左
用户可以通过向下滚动推进叙事时间，但壁画内容在视觉上可以向左运动。
必须理解：交互操作方向 ≠ 壁画叙事方向。
不得因为常规网页的左→右阅读习惯而擅自改变壁画的空间观看逻辑。

### 7. 文化遗产资料

不得凭空创造水龙祠历史、勾蓝瑶文化信息、壁画人物身份、仪式流程、图像象征意义、五猖相关内容、壁画观看顺序的历史解释。
AI 生成图像只能用于：
视觉辅助；
概念表现；
设计化重建；
动画素材辅助。
AI 生成结果不得未经验证直接作为真实文物或历史资料使用。
如果没有明确资料支持，标记为“待确认”；不得将推测写成事实。

### 8. 壁画原始素材

用户提供的高清壁画原始文件属于母版素材，不应直接修改或覆盖。
原始壁画文件可能达到 200–300 MB 以上。
如需优化性能，采用分层生成链路，不直接修改 Master：
原始 Master → 生成 Web 优化版本 → 生成多分辨率版本 → 生成 Tile
Web 端应优先采用：
多分辨率图像；
Deep Zoom；
图像切片；
Canvas/WebGL；
适当压缩的展示版本。
不得为了方便开发直接将所有超大原图一次性加载到页面。

### 9. 本地运行

项目以本地 Web 运行作为主要部署方式。
优先保证：
localhost 环境
下的完整运行。
不要默认依赖在线服务器、云端图片存储或外部 API。
如确实需要外部服务，必须明确说明原因。

### 10. 视觉与素材保护

不得未经明确要求：
修改用户提供的壁画人物；
改变壁画原始颜色；
添加不存在于原壁画中的人物或纹样；
用 AI 自动“修复”并替代真实壁画；
为追求视觉效果改变核心文化内容。
可以对素材进行裁切、缩放、遮罩、透明度、动效和展示层级处理，但必须保持原始内容可追溯。

### 11. 技术原则

技术服务于叙事：
优先考虑 HTML / CSS / JavaScript、SVG、Canvas、GSAP / ScrollTrigger、WebGL / Three.js（确有必要时）。
不得为了使用 3D、WebGL 或复杂动画而增加不必要的技术复杂度。
技术选择应优先考虑：性能、可维护性、本地运行稳定性和叙事效果。

性能：
重点关注首屏加载时间、高清壁画加载、内存占用、GPU 使用、滚动帧率、移动端兼容性、本地运行稳定性。
大型素材必须采用懒加载、分辨率控制或切片等策略。

修改现有代码：
修改前先理解现有代码结构；优先进行最小范围修改。
不得为了实现一个局部效果而无必要重构整个项目。
不得删除已有功能，除非用户明确要求或者有充分理由，并在说明中指出影响。

### 12. 代码组织原则

开发前先检查当前项目实际结构，不要假设当前项目一定已经存在 landing/ exploration/ shared/ 目录。
如果现有代码结构不同，优先适应现有结构，而不是为了符合文档示例而强行重构项目。
先识别：当前入口文件、当前路由、《入将图》交互入口、《入将图》核心组件、图片加载方式、样式系统、数据结构、构建工具、测试方式，然后再决定如何实现模块 A。

### 13. 当前开发优先级

当前优先级为：先完成模块 A 的水龙祠叙事引导页面。
模块 B《入将图》交互探索暂时视为已有核心成果 / 独立模块。
除非明确提出模块 B 的修改需求，否则不要主动修改模块 B。

## 工作方式

每次任务完成后，输出：
本次完成的修改；
测试与验证结果；
Git commit hash；
如存在未解决的问题或风险，明确列出。
如果需求存在事实错误、逻辑矛盾、技术不可行或信息不足，应先指出问题，再实施修改。
