# A08《入将图》探索交接：低保真实现与验收

日期：2026-09-25。范围仅为模块 A；模块 B 内部未改动。

A07 右端至左端巡视在 40 屏结束，A08 继续保留同一张 `mural-02-display.webp` 的左端画面和尺度。40—41 屏仅叠加简短说明与“进入探索”按钮，42 屏保持稳定。纵向滚动不触发跳转。按钮指向已由 `Mural-Exhibition/server.js` 确认的本地服务 `http://localhost:3000/index.html`；浏览器返回恢复 A08 滚动位置。

`module-a/a08/progress.mjs` 控制章节淡入，`controller.mjs` 负责文案和主动入口；A07 共享导读组件仅延长左端画面至 A08 结束，没有重播或重新请求图片。减少动态效果时入口仍可用。验证截图与结果在 `docs/validation/a08/`，覆盖 1920×1080、1440×900、1366×768、1024×768，A07→A08、反向、刷新、点击模块 B、浏览器返回及 reduced-motion。

此章不提供新的壁画释读；真实墙位与项目路线仍按主规划中的“设计示意”边界理解。
